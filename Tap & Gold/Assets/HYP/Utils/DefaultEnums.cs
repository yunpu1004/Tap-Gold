
public enum Direction { None, Left, Right, Up, Down }

public enum Axis { None, X, Y, XY }

public enum TweenStyle { None, Linear, Sin, Ease, EaseBack}

public enum DurationType { None, Temporary, Permanent }

public enum DecimalType { None, Point, Percent }

